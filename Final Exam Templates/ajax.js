(function(){

$(document).ready(

//If you want to add some setup properties use the code bellow
/*Source ==> http://api.jquery.com/jquery.ajax/ */
$.ajaxSetup(
    {
        url:"", /* url address */
        type: get, /* can be either get or post */
        dataType:	/* The data type expected of the server response.  From the many that can be chosen, two are more often during the exercises "json" or "text" */

    }
);


);

})();